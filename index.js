
const fs = require('node:fs')
/*
const content = {test1:"hammer",test2:"nail",test3:"board"}
fs.writeFile('test.json', JSON.stringify(content), err=>{console.error(err)})
*/
const imp =require("./test.json")
console.log(imp.test1)
// import * as rd from 'readline-sync'
// number.toFixed(x) = number até o x número depois da ,
// npm install -D @types/node
// enum //conteudo /index// conteudo = number /++1// conteudo = string /preencher todos
// for/ for of/ for in/ forEach()